# Research Library

An Obsidian-based research library for AI/ML papers, talks, and notes — with an automated pipeline that discovers, summarizes, and files papers into an indexed, interconnected knowledge base. The goal is consistent formatting and dense linking so Obsidian's graph view surfaces meaningful connections across topics.

This repo is both an Obsidian vault and a small Python toolchain. The vault root is the repo root.

## Layout

| Folder | What's in it |
|--------|--------------|
| `Library/` | Canonical knowledge base, organized by topic (not year). Each topic folder holds a semantic `MOC.md`, a machine-readable `index.json` of filed papers, and one note per paper. |
| `discovery/` | Daily auto-generated digests of recent papers, filtered by the Library topics. |
| `staging/` | Unfiled summaries, pipeline worklists, and discovery picks awaiting filing. |
| `scripts/` | Automation: daily discovery job, index rebuilder, discovery review UI. |
| `.claude/skills/` | The skills that drive the pipeline (discovery → summary → filing). |
| `docs/` | Code-development standards and execution plans. |
| `Notes_2024-2026/`, `Images_*/` | Legacy year-organized notes (migrating to `Library/`) and their images. |

## What's in the library

The library is organized by topic. The two largest families (VLA and World Models) are split into method-first sub-topics under an umbrella hub. The snapshot below is regenerated from each topic's `index.json` by `scripts/rebuild_index.py` — see [`ARCHITECTURE.md`](ARCHITECTURE.md#topics) for the full scope description of each topic.

<!-- BEGIN library-overview (auto-generated by scripts/rebuild_index.py — do not edit by hand) -->
355 papers across 26 topics with at least one filed paper. Counts below are regenerated from each topic's `index.json` by `scripts/rebuild_index.py`.

**Vision-Language-Action models** — generalist robot policies
| Topic | Papers |
|-------|:------:|
| `VLA — Foundations & Training` | 29 |
| `VLA — Reasoning, Planning & Inference` | 24 |
| `VLA — Spatial Grounding & Embodiment` | 18 |
| `VLA — Evaluation, Benchmarks & Safety` | 11 |

**World Models** — learned models of environment dynamics
| Topic | Papers |
|-------|:------:|
| `World Models — Control & Decision-Making` | 16 |
| `World Models — Video Generation` | 10 |
| `World Models — Interactive Simulation` | 11 |
| `World Models — Object-Centric` | 8 |

**3D & spatial perception**
| Topic | Papers |
|-------|:------:|
| `Open-Vocabulary Vision` | 15 |
| `SLAM` | 12 |
| `Semantic Mapping` | 8 |
| `3D Reconstruction — Feed-Forward` | 9 |
| `3D Generation` | 8 |

**Reasoning, language & embodiment**
| Topic | Papers |
|-------|:------:|
| `Multimodal Reasoning` | 24 |
| `Joint Embedding Predictive Architectures` | 9 |
| `Vision-Language Navigation` | 9 |
| `Human-to-Robot Demonstration Transfer` | 26 |
| `Humanoid Control` | 22 |

**Other topics**
| Topic | Papers |
|-------|:------:|
| `Grasping & Dexterous Manipulation` | 29 |
| `Inference-Time Reasoning Algorithms` | 11 |
| `LLM Agents & Agentic AI` | 5 |
| `Memory & Retrieval` | 7 |
| `Multi-Agent Coordination` | 6 |
| `Reward Learning from Video` | 2 |
| `Robotics — Surveys & Reviews` | 4 |
| `VLA — Post-Training & Data` | 22 |
<!-- END library-overview -->

## The pipeline

Papers flow through four stages, each backed by a Claude Code skill:

1. **Discover** (`paper-discovery`) — A daily `launchd` job fetches recent HuggingFace papers and filters them by topic (BM25 keywords + semantic similarity against each topic's `MOC.md`), writing a digest to `discovery/`.
2. **Triage** (`scripts/launch_review.sh`) — A local-only web UI for visually picking papers from the digests; selections land in `staging/`.
3. **Summarize** (`arxiv-summary`) — Generates a Heilmeier-style summary with YAML frontmatter from an arXiv link or PDF.
4. **File** (`library-filing`) — Moves a summary into the right `Library/<Topic>/`, stamps its `topic`, and registers it in that topic's `index.json`.

For bulk work, `pipeline-orchestrator` runs all four stages across many papers from a single prompt (parallel summaries, serial filing).

## Common tasks

```bash
# Scan for new papers (inside a Claude Code session)
/paper-discovery                 # or: /paper-discovery days=3

# Launch the discovery review UI on localhost
bash scripts/launch_review.sh    # optional [port], default 8000

# Rebuild every Library/<Topic>/index.json from note frontmatter
source researchEnv/bin/activate
python scripts/rebuild_index.py            # add --check to report drift without writing
```

## Conventions (quick reference)

- **New notes** go in `Library/<Topic>/`, named `YYYY-MM-DD Paper Name.md`, opening with a YAML frontmatter block. Never create notes in the year-based folders.
- **Linking:** `[[Paper Name]]` for notes, `[[concept]]` for concept notes. A note is registered in `index.json`, not by editing the MOC.
- **MOCs are semantic only** — topic scope + curated concept links; they do not enumerate papers.
- **Images** live in `Images_YYYY/` and embed with `![[filename.png]]`; they resolve vault-wide by filename, so moving (not renaming) them is safe.

## Documentation

- **[`ARCHITECTURE.md`](ARCHITECTURE.md)** — the canonical reference: folder map, Library system, note format, discovery pipeline, and automation setup. **Read this before making changes.**
- **[`CLAUDE.md`](CLAUDE.md)** — guidance for Claude Code, including the code-development workflow.
- **`docs/design-docs/`** — engineering standards (design, reliability, security, testing, frontend).

## Environment

Python tooling uses the `researchEnv/` virtualenv (gitignored): `source researchEnv/bin/activate`.
